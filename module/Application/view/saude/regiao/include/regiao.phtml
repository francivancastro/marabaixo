<?php 
$sm = $this->getHelperPluginManager()->getServiceLocator();
$app = $sm->get("AplicacaoFachada");

//validando região atual
$xml = $app->getRegiaoAtual();
$result = \Marabaixo\Util::xml_to_array($xml);

$regiao_atual = false;
if (isset($result["data"]) && !empty($result["data"])) {
    $regiao_atual = $result["data"];
    $txt_regiao_atual = "--";
    if (isset($regiao_atual["tostring"])) {
        $txt_regiao_atual = $regiao_atual["tostring"];
    }
?>
<div class="box">
    <div class="box-header with-border">
        <h3 class="box-title">Região Atual</h3>
    </div><!-- /.box-header -->
    <div class="box-body">
        <dl class="dl-horizontal">
            <dt>Região Atual:</dt>
            <dd><?php echo $txt_regiao_atual; ?></dd>
        </dl>
    </div>
</div>
<?php }